# P6 Concurrent Review — Cross-Cutting Audit of the RouteIQ-on-AWS Re-architecture

**Snapshot audited:** commit `7c57eba` (P0 + P1 + P2 squash-merges).
**Note:** working-tree `deploy/` is byte-identical to `7c57eba` (the only delta from HEAD `0ce0178` is `.seeds/issues.jsonl`), so the audit ran directly against it.
**Scope:** cross-cutting gaps the per-wave reviews could not see (each only saw its own stack) + whole-system coherence.

## Gate verification

- **Combined cred-free gate GREEN: `180 passed` in 15.39s, no AWS creds** (`uv sync --extra cdk && uv run python -m pytest deploy/cdk/tests/ -q`). Matches the claimed P0 36 + P2 81 + P1 63 = 180 exactly.
- `helm lint` passes (0 failed); `helm template` (default + `grafanaDashboard`) renders clean.
- **Combined-app cdk-nag is CLEAN:** synthesised all three stacks in ONE app with `state + obs + amg + lake` all on; FOUNDATION and OBS report **0 unsuppressed AwsSolutions errors / 0 ValidationFailures**. STATE synthesises and all its cross-stack imports resolve (the `find_error` jsii `MetadataEntry` KeyError on the second cross-coupled stack is a jsii-version quirk, not a finding; `test_state_cdk_nag.py` is 0-finding).

## What hangs together correctly

- **P0 → P1 cross-stack wiring is sound.** `RouteIqStateStack` emits 14 `Fn::ImportValue` against exactly the 7 P0 exports (VPC, PodSg, 2× private-data + 2× private-app subnets, PodRole) — **UNRESOLVED imports: NONE**. The `PodStateGrants` policy correctly applies BOTH `rds-db:connect` and `elasticache:Connect` to the pod role, using the own-`iam.Policy`-in-state-stack pattern to dodge the DependencyCycle. This is the *good* half of the grant story.
- Context-key naming is consistent (no undeclared/unused `routeiq:` keys). The `_COLUMNS` routing-decision schema is in-sync between src and CDK *today*. Chart README CDK-output names match the actual `CfnOutput`s and honestly flag the unbuilt runtime half.

## Headline NEW cross-cutting findings (seed-worthy, not in the ~17 filed)

| # | Severity | Finding |
|---|----------|---------|
| 1 | **HIGH (P2)** | **Pod role never gets `appconfig:Get*` from ANY stack.** Unlike `aps:RemoteWrite` (which at least has an uncalled `amp_remote_write_grant` seam), AppConfig has **no grant method at all** — `config_state_construct.py` has zero grant helpers, and the obs stack takes no `pod_role`. A pod polling AppConfig (ADR-0026) gets AccessDenied even after all 3 stacks deploy. (Filed `RouteIQ-4333` is about app-side retrieval *code*, not the IAM grant.) |
| 2 | **HIGH (P2)** | **P2 → P0 log-group coupling is NAME-only — no `Fn::ImportValue`, no `add_dependency`.** CloudFormation cannot enforce P0-before-P2; the P2 `MetricFilter`/`SubscriptionFilter` CREATE fails if the P0 group isn't present. P1's Export/ImportValue *does* enforce ordering; P2's does not. Unenforced deploy-ordering hazard. |
| 3 | **HIGH (P2)** | **AppConfig validator Lambda dual-resolution → gate/deploy DRIFT.** `shutil.which("docker")` checks the binary, not a healthy daemon. Docker-absent → inline `return None` **accept-all no-op** validator; docker-present → real YAML/shape/secret-deny validator + a different template + a different asset hash. ALL tests + CI exercise only the no-op; a docker-equipped deploy ships a different template; a docker-binary-present-but-daemon-down host **crashes synth** (reproduced). The security-relevant inline-secret-deny gate is silently absent on docker-less deploy hosts. |
| 4 | MEDIUM (P3) | **No P2 snapshot test.** Only P0 `dev.json` + P1 `state-dev.json` baselines exist; the full `RouteIqObservabilityStack` template is never pinned against drift (and can't be deterministically, given #3). |
| 5 | MEDIUM (P3) | **Routing-log-group name TRIPLICATED** as independent literals (`eks_cluster_construct.py:412`, `nag_suppressions.py:331-332`, `routeiq_observability_stack.py:117`) relying on the unstated `cluster_name == routeiq-<env>` invariant. Distinct from the filed `_COLUMNS` duplication (`RouteIQ-199e`). |
| 6 | MEDIUM (P3) | **Chart has no seam to consume P2 `AmpRemoteWriteUrl`/`AppConfigArn` outputs** (no ADOT sidecar, no README wiring), and **emits no `AWS_REGION`** — a single shared prerequisite across Bedrock (P0), the RDS/cache SigV4 token signer (P1), and AMP remote-write (P2). The cross-plane breadth is only visible synthesising all 3 waves. |
| 7 | LOW (P4) | **IRSA-vs-Pod-Identity doc drift extends beyond ADR-0030** into ADR-0026/0027/0028/0029 body text + cross-links (`ADR-0027:43`, `ADR-0028:56,104`, all the `0030-...-irsa-substrate.md` links). The code uses Pod Identity (no OIDC). Filed `RouteIQ-1f1b` is ADR-0030-scoped only. |

## Confirmed (already filed) — verified against source, not double-filed

`RouteIQ-731c` (`$.level` error filter never fires — emitter has no `level` key, verified end-to-end), `RouteIQ-8f08` (stale `$.msg`/`$.routing_latency_ms` P0 prep filter), `RouteIQ-74c0`/`717b` (`amp_remote_write_grant` never called), `RouteIQ-8374` (bootstrap Lambda SG `replay_store_sg` blocked by P0 `vpce_sg` 443-from-`pod_sg`-only), `RouteIQ-d3a4` (app-side IAM-token minting unbuilt), `RouteIQ-4b2a` (ADR-0027 claims 9 alarms, 3 built), `RouteIQ-199e` (`_COLUMNS` duplication), `RouteIQ-df6e` (STS endpoint dropped on Pod Identity path will bite the P1 rds-db token signer).

## Tally

- **9 new findings; 7 seed-worthy** (3 HIGH/P2, 3 MEDIUM/P3, 1 LOW/P4), 2 info.
- **5 confirmations** of already-filed seeds (verified, not re-filed).
- The system **hangs together for P0→P1**; the cross-cutting weakness is the **P2 boundary**: two of four promised pod-role grants (`aps:RemoteWrite`, `appconfig:Get*`) never reach the role, the P2↔P0 log-group coupling is an unenforced name-string, and the validator-Lambda docker dual-resolution makes the cred-free gate validate a no-op that diverges from the deployed artifact.
